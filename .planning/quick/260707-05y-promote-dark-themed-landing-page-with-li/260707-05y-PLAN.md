---
phase: quick-260707-05y
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/routes/+page.svelte
  - src/routes/new-12345/+page.svelte
autonomous: true
requirements: [QUICK-PROMOTE-DARK]
must_haves:
  truths:
    - "Visiting / renders the dark-themed landing page with a working light/dark toggle switch (Sun/Moon)."
    - "Registering from / POSTs source: '/' to the Apps Script endpoint (no longer '/new-12345')."
    - "The 'Email me about new releases and updates' consent checkbox is checked by default, and returns to checked after a successful submit reset."
    - "Navigating to /new-12345 returns 404 (route removed)."
  artifacts:
    - src/routes/+page.svelte
  key_links:
    - "appsScriptUrl imported from $lib/registration and used by handleFormSubmit"
    - ".spc-v2 wrapper class present so global theme CSS in src/app.css applies"
---

<objective>
Promote the dark-themed landing page (currently the private prototype at `src/routes/new-12345/+page.svelte`) to be the main `/` route, then remove the prototype route. This is a PROMOTION, not a redesign: copy the dark+switch design verbatim and apply exactly two behavioral adjustments (registration `source` value and consent-checkbox default), plus de-prototype the page head.

Purpose: Ship the finished dark design (with its built-in light/dark toggle) as the public landing page.
Output: `src/routes/+page.svelte` replaced with the promoted design; `src/routes/new-12345/` deleted.
</objective>

<execution_context>
@/Users/kirandev/Projects/stock-plan-companion/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@./.claude/CLAUDE.md
@src/routes/new-12345/+page.svelte
@src/routes/+page.svelte
@src/lib/registration.ts
</context>

<constraints>
- **This is a promotion, not a redesign.** Do NOT introduce any new behavior, restyle, refactor, or "improve" the design beyond the changes listed below.
- **Preserve the registration wiring EXACTLY** as it exists in `new-12345/+page.svelte`:
  - Keep `import { appsScriptUrl } from '$lib/registration';`
  - Keep the CORS-simple request: `method: 'POST'`, `headers: { 'Content-Type': 'text/plain;charset=utf-8' }`, JSON string body, and NO `mode: 'no-cors'`.
  - Keep the `{ok}`-based failure detection (`response.ok && parsed.ok === true`), including the non-JSON parse fallback.
  - Keep the `.spc-v2` wrapper + `class:dark`, the `theme-v2` localStorage key, and the default-dark theme. The theme CSS is global in `src/app.css` (keyed on the `.spc-v2` class), so no CSS changes are needed.
- **Tooling note (do NOT fix):** `npm run lint` runs `prettier --check`, which crashes on a PRE-EXISTING prettier / prettier-plugin-svelte version bug affecting ALL `.svelte` files. Do NOT attempt to fix that crash and do NOT run `npm run lint`. For lint verification use `npx eslint <files>` directly on the changed files only.
- Follow project style: tabs, single quotes, no trailing commas, 100-char width. No `Co-Authored-By` trailer in commits.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Promote the dark+switch design to the main / route</name>
  <files>src/routes/+page.svelte</files>
  <action>
Replace the ENTIRE contents of `src/routes/+page.svelte` with the current contents of `src/routes/new-12345/+page.svelte`, then apply exactly these three edits to the promoted file (and nothing else):

1. **Registration source (CRITICAL).** In `handleFormSubmit`, change the payload field `source: '/new-12345'` to `source: '/'`. Without this, every registration from the live homepage is mislabeled `/new-12345` in the Google Sheet.

2. **Consent checkbox defaults ON.** The consent checkbox (`<input type="checkbox" name="consent" required ...>`, label "Email me about new releases and updates.") currently renders unchecked. Add the `checked` attribute to that input so it defaults to checked. The form uses uncontrolled inputs read via `FormData` and calls `form.reset()` on a successful submit — the HTML `checked` attribute IS the field's default state, so `form.reset()` returns the box to checked automatically. Do NOT add any new JS state or reactive variable for this; adding the `checked` attribute is the complete change. Keep `required` as-is.

3. **De-prototype the page head.** The prototype `<svelte:head>` contains `<title>Stock Plan Companion — preview</title>` and `<meta name="robots" content="noindex, nofollow" />`. Change the title to `Stock Plan Companion` (drop the "— preview" prototype marker) and REMOVE the `robots` noindex/nofollow meta entirely — the public homepage must be indexable. Leave the rest of the head untouched. (These are prototype-only markers; removing them is part of promotion, not a redesign.)

Leave all other markup, styling, imports, snippets, ticker logic, screenshot gallery, and the `.spc-v2` theme toggle exactly as they are in the source file.
  </action>
  <verify>
- `grep -n "source: '/'" src/routes/+page.svelte` returns the payload line (and `grep -n "new-12345" src/routes/+page.svelte` returns nothing).
- `grep -n "appsScriptUrl" src/routes/+page.svelte` shows the import from `$lib/registration` and its use in `fetch(appsScriptUrl, ...)`.
- `grep -n "text/plain;charset=utf-8" src/routes/+page.svelte` confirms the CORS-simple content type is preserved.
- `grep -n 'name="consent"' src/routes/+page.svelte` shows the input now includes `checked`.
- `grep -n "robots" src/routes/+page.svelte` returns nothing; `grep -n "<title>" src/routes/+page.svelte` shows `Stock Plan Companion` with no "preview".
- `npx eslint src/routes/+page.svelte` passes (do NOT run `npm run lint`).
- `npm run check` reports 0 errors.
  </verify>
  <done>
`/` renders the dark themed landing page with the working light/dark toggle; the registration POST body sends `source: '/'`; the consent checkbox is checked by default and returns to checked after a successful submit reset; the head no longer carries the `— preview` title or the `noindex, nofollow` meta; registration wiring (import, text/plain request, `{ok}` detection) is byte-for-byte preserved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove the prototype route directory</name>
  <files>src/routes/new-12345/+page.svelte</files>
  <action>
Delete the entire `src/routes/new-12345/` directory (including `+page.svelte` and any sibling files). The route is intentionally removed — a 404 at `/new-12345` is the desired outcome, since it was a private prototype URL. Use `rm -rf src/routes/new-12345`.
  </action>
  <verify>
- `test ! -d src/routes/new-12345 && echo GONE` prints `GONE`.
- `grep -rn 'new-12345' src/` returns nothing (no lingering references anywhere in src).
- `npm run build` succeeds (confirms the removed route breaks no prerender/link, and the promoted `/` builds cleanly).
  </verify>
  <done>
`src/routes/new-12345/` no longer exists, no `new-12345` references remain in `src/`, and the static build completes successfully.
  </done>
</task>

</tasks>

<verification>
Run after both tasks:
- `npm run check` → 0 errors.
- `npm run build` → succeeds.
- `grep -rn 'new-12345' src/` → no output.
- `grep -n "source: '/'" src/routes/+page.svelte` → confirms the `/` handler POST body now sends `source: '/'`.
- `npx eslint src/routes/+page.svelte` → passes. (Do NOT run `npm run lint`; its `prettier --check` step crashes on a pre-existing plugin bug that is out of scope.)
</verification>

<success_criteria>
- Visiting `/` shows the dark-themed page with the Sun/Moon light/dark toggle (defaults dark, remembers via `theme-v2`).
- Registration from `/` POSTs `source: '/'` to the Apps Script endpoint using the unchanged text/plain CORS-simple request and `{ok}` failure detection.
- Consent checkbox is pre-checked and returns to checked after a successful submit reset.
- `/new-12345` is removed (404), and no `new-12345` reference remains in `src/`.
- `npm run check` and `npm run build` both pass.
</success_criteria>

<output>
Create `.planning/quick/260707-05y-promote-dark-themed-landing-page-with-li/260707-05y-SUMMARY.md` when done.
</output>
