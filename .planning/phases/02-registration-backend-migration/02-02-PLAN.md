---
phase: 02-registration-backend-migration
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: []
autonomous: false
requirements: [REG-02, REG-04]

must_haves:
  truths:
    - A real submission on each route lands a new row in the Google Sheet with the correct source value and a server-side timestamp.
    - The Apps Script response body is readable JSON (not opaque) so a failed submission surfaces an inline error while keeping the typed email.
    - No formspree.io reference remains anywhere in src/.
  artifacts: []
  key_links:
    - The CORS simple-request POST (text/plain, no mode:no-cors) keeps the redirected response body readable — this is what makes failure detection (REG-04) real rather than always-success.
    - D-10: The live end-to-end check (Task 2) exercises the already-provisioned Apps Script Web App + "Registrations" Google Sheet — creation/deployment is complete, so this plan proves the provisioned endpoint works rather than provisioning it.
---

<objective>
Prove the migrated registration flow end-to-end and lock it against regression. This closes RESEARCH Assumption A2 / Open Question 1 — the one mechanic (Apps Script's `/exec` 302 redirect returning a browser-readable body) that could not be confirmed against official docs and must be verified against the live deployment. It also runs the full local gate to confirm zero Formspree references remain and the static site still builds.

Purpose: Confirm REG-02 (rows actually land, no cap) and REG-04 (failed submission shows an inline error without discarding the email) against the real deployed endpoint.
Output: No source changes — a green automated gate plus a recorded live end-to-end verification.
</objective>

## Phase Goal

**As a** prospective Stock Plan Companion user, **I want to** have my registration reliably reach the owner's Google Sheet, **so that** my sign-up is never silently dropped and I still see the download.

<execution_context>
@/Users/kirandev/Projects/stock-plan-companion/.claude/gsd-core/workflows/execute-plan.md
@/Users/kirandev/Projects/stock-plan-companion/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-registration-backend-migration/02-CONTEXT.md
@.planning/phases/02-registration-backend-migration/02-RESEARCH.md

# The deployed endpoint + the curl-vs-browser caveat
@/Users/kirandev/.claude/projects/-Users-kirandev-Projects-stock-plan-companion/memory/apps-script-registration-endpoint.md

# The wired handlers + config from plan 02-01
@src/lib/registration.ts
@src/routes/+page.svelte
@src/routes/new-12345/+page.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full local gate — clean cutover, contract test, type-check, build</name>
  <files></files>
  <read_first>
    - src/lib/registration.ts (the endpoint constant to confirm is imported, not inlined)
    - src/routes/+page.svelte and src/routes/new-12345/+page.svelte (post-migration handlers)
    - .planning/phases/02-registration-backend-migration/02-CONTEXT.md (D-01 hard cutover — no Formspree fallback)
  </read_first>
  <action>
    Run the automated verification gate; expect no source changes. Confirm the Formspree cutover is complete (no `formspree` string anywhere under `src/`), the endpoint contract test is green, type-check and lint are clean, and the static build succeeds. If any command fails, fix the defect in the owning file from plan 02-01 (do not introduce new behavior) and re-run.
  </action>
  <verify>
    <automated>! grep -rn 'formspree' src/ && npx vitest run src/lib/registration.test.ts && npm run check && npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'formspree' src/` returns nothing (D-01 hard cutover confirmed).
    - `npx vitest run src/lib/registration.test.ts` passes.
    - `npm run check`, `npm run lint`, and `npm run build` all succeed.
  </acceptance_criteria>
  <done>The repo builds cleanly with the endpoint contract test green and zero Formspree references.</done>
</task>

<task type="auto">
  <name>Task 2: Live end-to-end submit on both routes — confirm capture, reveal, and failure handling</name>
  <files></files>
  <read_first>
    - /Users/kirandev/.claude/projects/-Users-kirandev-Projects-stock-plan-companion/memory/apps-script-registration-endpoint.md (deployed URL, `{ok:true}` contract, and the curl-mishandles-redirect caveat)
    - .planning/phases/02-registration-backend-migration/02-RESEARCH.md (Open Question 1 / Assumptions A2 — the redirect-readability unknown to confirm)
    - .planning/phases/02-registration-backend-migration/02-CONTEXT.md (the 4 success criteria)
  </read_first>
  <action>
    Start the site with `npm run dev` and exercise a real submission on both routes in a browser (NOT curl — curl mishandles the Apps Script redirect and returns a misleading 405/Drive page per the memory note; a real browser `fetch` reads the response correctly). This is the runtime confirmation of A2 that research could not close against official docs, so it is a human check, not an automated assertion. Make no source changes.
  </action>
  <verify>
    <human-check>
      1. On `/`: enter an email (plus an optional name; toggle the consent box), submit. In the Network tab, the POST to the `/exec` URL completes and its response body is readable JSON `{ ok: true }` (status is a real 200-class, not `0`/opaque). The download options reveal in place and the form clears (REG-01, REG-03).
      2. A new row appears in the "Registrations" Google Sheet with `source` = `/`, a server-side timestamp, and the submitted name/email/consent (REG-02).
      3. Repeat on `/new-12345`: a row lands with `source` = `/new-12345`, and the download-reveal (including the existing downloadLinks block) still works.
      4. Failure path: go offline (or point the endpoint at a bad URL via devtools) and submit — an inline error appears and the typed email stays in the field (REG-04). No CORS/preflight error appears in the console on the happy path.
    </human-check>
  </verify>
  <acceptance_criteria>
    - Rows land in the Sheet for both routes with the correct `source` and a server timestamp.
    - The `/exec` response body is browser-readable JSON `{ok:true}` (A2 confirmed — not opaque, no `mode:no-cors`).
    - The failure path preserves the typed email and shows an inline error (REG-04).
    - No console CORS/preflight error on the happy path (D-03 simple-request confirmed).
  </acceptance_criteria>
  <done>All four Phase-2 success criteria are observed live on both routes; the redirect-readability assumption (A2) is confirmed against the real deployment.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser client -> Google Apps Script Web App | Anonymous public `/exec` endpoint; this plan verifies (does not change) the boundary established in 02-01. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-04 | Repudiation / Information Disclosure | Live end-to-end test writes real rows to the production "Registrations" Sheet | low | accept | Verification uses obviously-marked test values (e.g. a `verify+e2e@` style email); rows are appended, not destructive, and the sheet owner can delete test rows afterward. No secret is exposed by the test. |

**Package Legitimacy:** Not applicable — verification-only plan; no packages installed.
</threat_model>

<verification>
- Automated gate (Task 1): grep-clean of `formspree`, contract test, `check`, `lint`, `build` all green.
- Live gate (Task 2): rows land in the Sheet for both routes with correct `source`; response body readable `{ok:true}`; failure path keeps the email and shows an inline error.
</verification>

<success_criteria>
- REG-02 proven: submissions land as Sheet rows via the Apps Script endpoint with no cap.
- REG-04 proven: a failed submission shows an inline error without discarding the entered email.
- The migration builds and lints clean with zero Formspree references remaining.
</success_criteria>

## Artifacts this phase produces

- No new source artifacts — this plan verifies the artifacts produced by 02-01 (`src/lib/registration.ts`, both migrated `handleFormSubmit` handlers).
- A recorded end-to-end verification result confirming RESEARCH Assumption A2 (readable `/exec` redirect response) against the live deployment.

<output>
Create `.planning/phases/02-registration-backend-migration/02-02-SUMMARY.md` when done.
</output>
